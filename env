# --------------------------------------------------------------------
# Exemplo de Arquivo de Configuração
#
# Este arquivo pode ser usado como ponto de partida para seus próprios
# arquivos .env personalizados e contém a maioria das configurações possíveis
# disponíveis em uma instalação padrão.
# --------------------------------------------------------------------


# --------------------------------------------------------------------
# ENVIRONMENT (AMBIENTE)
# Preencha com 'production' para produção e 'development' para desenv.
# --------------------------------------------------------------------

CI_ENVIRONMENT = production

# --------------------------------------------------------------------
# APP (APLICATIVO)
# Informações gerais sobre o Aplicativo
# --------------------------------------------------------------------

app.name = iBlood
app.baseURL = http://localhost:8080


# --------------------------------------------------------------------
# DATABASE (BANCO DE DADOS)
# Preencha abaixo, os dados para conexão com o banco de dados
# --------------------------------------------------------------------

database.default.hostname = localhost
database.default.database = iblood
database.default.username = root
database.default.password = root
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306

# --------------------------------------------------------------------
# ENCRYPTION (ENCRIPTAÇÃO)
# --------------------------------------------------------------------

encryption.key =
